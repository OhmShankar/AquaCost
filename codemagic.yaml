workflows:
  ios-capacitor:
    name: Capacitor iOS Build
    environment:
      vars:
        NODE_VERSION: "18"
    scripts:
      - npm install
      - npm run build
      - npx cap sync ios
    xcode:
      project_path: ios/App/App.xcworkspace
      scheme: App
      export_method: development
